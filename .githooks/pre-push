#!/usr/bin/env bash
set -euo pipefail

branch="$(git rev-parse --abbrev-ref HEAD)"

if [[ "$branch" == "main" || "$branch" == "master" ]]; then
  echo "[pre-push] ❌ Direct pushes to '$branch' are blocked. Push a feature branch and open a PR."
  exit 1
fi
